{plugins, [rebar3_format]}.

{format, [
    {files, ["src/*.erl", "test/*.erl"]},
    {formatter, default_formatter},
    {options, #{inline_attributes => {when_under, 1},
                inline_items => {when_under, 4}}}
]}.
